# Terraform openstack keypair
# ----------------------------------------------------------------------------
resource "openstack_compute_keypair_v2" "{{ item_keypair.name }}" {
  name       = "{{ item_keypair.name }}"
  public_key = "{{ item_keypair.public_key_file }}"
}
